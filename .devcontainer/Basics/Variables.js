const accountId = '12345'
var accountMail = 'n@gmail.com'
let accountPw = '23456'

// we should not use var now in java script because it causes problem with the scopes. scope is {when we use the function}

console.table([accountId, accountMail, accountPw]);